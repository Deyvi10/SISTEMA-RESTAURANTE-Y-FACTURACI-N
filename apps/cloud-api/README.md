# cloud-api

API REST multi-tenant y workers (fiscal, correo, sync) en Go. **Empieza en la Fase 1** (F1-01).

- Binarios: `cmd/api`, `cmd/worker`.
- Dominio en `internal/<dominio>/{domain,app,adapters}`.
- Contrato: `contracts/openapi/`.
